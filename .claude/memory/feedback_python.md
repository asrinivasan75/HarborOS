---
name: Use python not python3
description: User prefers `python` command over `python3` in all contexts
type: feedback
---

Use `python` instead of `python3` for all commands.

**Why:** User explicitly requested it.
**How to apply:** Always use `python` in bash commands, never `python3`.
